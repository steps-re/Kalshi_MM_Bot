# Builds from the repo root so the image can include src/ alongside the app.
steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - analysis_app/Dockerfile
      - -t
      - ${_IMAGE}
      - .
images:
  - ${_IMAGE}
options:
  logging: CLOUD_LOGGING_ONLY
